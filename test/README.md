# Task 7 pilot workspace

This directory is reserved for the bounded end-to-end pilot. The pilot will create a small, reviewable artifact here so the normal ChatGPT → Codex → Git verification loop can be exercised without touching application code.
